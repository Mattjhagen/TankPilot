<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="androidx.car.app.MAP_TEMPLATES" />

    <application
        android:name=".TankPilotApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TankPilot"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoTitleBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Android Auto entry point. Category POI: TankPilot surfaces fuel status and
          gas-station discovery, not turn-by-turn navigation (see phases/phase-03a-android-auto-foundation.md).
        -->
        <service
            android:name=".auto.TankPilotCarAppService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="TankPilot">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.POI" />
            </intent-filter>
        </service>
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />

        <!--
          Required in addition to the POI category + minCarApiLevel above — the host
          validator (CAR.VALIDATOR) independently checks for this descriptor and
          rejects the package with "Uses for TEMPLATE not defined" without it. See
          res/xml/automotive_app_desc.xml and phases/phase-03a-android-auto-foundation.md §5.
        -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
    </application>

</manifest>
