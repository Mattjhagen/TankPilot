import SwiftUI

struct HistoryScreen: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("Trip History (Placeholder)")
            }
            .navigationTitle("History")
        }
    }
}
